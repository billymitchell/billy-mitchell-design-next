import React from "react"

const Separator = ({ className }) => {
  return (
    <div className="separator bg-transparent">
      <div id="skewed" className={className}></div>
    </div>
  )
}

export default Separator
